# iCloud 和 CloudKit 設定指南

## 完成這些步驟以啟用 iCloud 同步功能：

### 1. 在 Xcode 中配置專案設定

1. **打開 Xcode 專案**
2. **選擇專案檔案** (InvestmentDashboard)
3. **選擇 Target** (InvestmentDashboard)
4. **點擊 "Signing & Capabilities" 選項卡**

### 2. 添加 iCloud Capability

1. **點擊 "+ Capability" 按鈕**
2. **搜索並添加 "iCloud"**
3. **在 iCloud 設定中，勾選以下選項：**
   - ✅ CloudKit
   - ✅ CloudKit Database

### 3. 添加 Background Modes (可選，用於後台同步)

1. **再次點擊 "+ Capability" 按鈕**
2. **搜索並添加 "Background Modes"**
3. **勾選：**
   - ✅ Background processing

### 4. 配置 CloudKit Container

1. **在 iCloud 設定中，點擊 "CloudKit" 旁邊的下拉選單**
2. **確保有一個 Container 被選中**（通常會自動創建一個）
3. **如果需要，可以點擊 "CloudKit Dashboard" 按鈕來管理 Container**

### 5. Core Data Model 配置

✅ **已完成** - Core Data Model 已經配置為支援 CloudKit：
- 創建了 `DataModel.xcdatamodeld`
- 設定了 `Customer` 實體
- 啟用了 CloudKit 同步 (`usedWithCloudKit="YES"`)

### 6. 代碼配置

✅ **已完成** - 以下組件已經創建：
- `PersistenceController.swift` - Core Data + CloudKit 容器
- `SidebarView.swift` - 客戶列表側邊欄
- `CustomerDetailView.swift` - 客戶詳情視圖
- `AddCustomerView.swift` - 新增客戶表單
- `EditCustomerView.swift` - 編輯客戶表單

### 7. 測試步驟

要測試 iCloud 同步功能：

1. **確保您已登錄 iCloud 帳號**
2. **在一個設備上運行 App 並添加客戶**
3. **在另一個使用相同 iCloud 帳號的設備上運行 App**
4. **檢查客戶資料是否自動同步**

### 8. 支援的平台

此 App 支援：
- 📱 iPhone (所有尺寸)
- 📱 iPad (所有尺寸)
- 🔄 自動適應橫向和直向模式
- ☁️ iCloud 跨設備同步

### 9. UI 特色

- **側邊欄設計**：使用 `NavigationSplitView` 在 iPad 上顯示側邊欄，在 iPhone 上作為推送導航
- **響應式佈局**：自動適應不同屏幕尺寸
- **iCloud 同步**：所有客戶資料自動在設備間同步

### 重要提醒

- **Apple Developer Account**：需要付費的 Apple Developer Account 才能使用 CloudKit
- **iCloud 登錄**：用戶必須在設備上登錄 iCloud 才能使用同步功能
- **網路連接**：需要網路連接才能進行 iCloud 同步

### 故障排除

如果同步不工作：
1. 檢查設備是否登錄了 iCloud
2. 確認 App 有 iCloud 權限
3. 檢查網路連接
4. 在設定 > Apple ID > iCloud 中確認 App 已啟用 iCloud